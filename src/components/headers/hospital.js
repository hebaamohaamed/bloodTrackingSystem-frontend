import {useHistory} from 'react-router-dom'

function HospitalHeader() {
  let history = useHistory();
    return (
      <div>
        <h1>Hospital Header</h1>
      </div>
    );
  }
  
  export default HospitalHeader;